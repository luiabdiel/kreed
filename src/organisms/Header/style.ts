import styled from "styled-components"

export const Header = styled.header`
    padding-top: 30px;

    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
`