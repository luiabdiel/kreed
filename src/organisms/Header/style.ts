import styled from "styled-components"

export const Header = styled.header`
    height: 42px;

    margin-top: 30px;

    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
`